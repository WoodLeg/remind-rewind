(function() {
    'use strict';

    angular
    .module('remindRewind.authentication', [
        'remindRewind.user'
    ])
    .config(config);

    config.$inject  = [];

    function config() {
        // Define states here
    }

})();
